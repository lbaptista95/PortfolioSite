import React from 'react';


export default function ProjectItem(props) {

    const {name,description,mediaList,skillNames,onChange} = props
    
    return (
        <div className='project-item'>
          
        </div>
      );
}